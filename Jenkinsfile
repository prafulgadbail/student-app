pipeline {
    agent {
        label 'build-agent'
    }

    stages {
        stage('Initialize') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }

        }

    }

}